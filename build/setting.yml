add_needed:
  - /tmp/lpmx_test/bin/main:
    - /tmp/lpmx_test/lib/getpid.so

add_rpath:
  - /tmp/lpmx_test/lib

user_shell: /usr/bin/zsh

allow_list:
  - bash: all
  - touch:
    - /tmp/test
    - /tmp/test2
